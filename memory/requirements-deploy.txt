# RW-1: pinned (==) to the versions verified locally 2026-09-25 (py3.12, CPU).
# torch is installed separately in the Dockerfile from the CPU-only index (torch==2.14.0+cpu).
# Hash pinning (--require-hashes) not yet applied: needs a lock generated on linux/amd64 py3.12.
fastapi==0.141.1
starlette==1.7.0
uvicorn[standard]==0.53.0
pydantic==2.13.5
pydantic-settings==2.15.0
httpx==0.28.1
numpy==2.5.3
sentence-transformers==6.1.0
transformers==5.17.0
huggingface-hub==1.33.0
tokenizers==0.23.2
safetensors==0.8.0
scikit-learn==1.9.1
scipy==1.18.1
